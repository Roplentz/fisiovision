import{describe,expect,it}from"vitest";import{runPilatesHundredProtocol,type Landmark,type PoseFrame}from"../src/index.js";
function frame(timestampMs:number,wristY:number,asymmetry=0,visibility=.99):PoseFrame{const landmarks:Landmark[]=Array.from({length:33},()=>({x:.5,y:.5,visibility}));landmarks[7]={x:.42,y:.22,visibility};landmarks[8]={x:.46,y:.22,visibility};landmarks[11]={x:.4,y:.35,visibility};landmarks[12]={x:.48,y:.35,visibility};landmarks[15]={x:.3,y:wristY,visibility};landmarks[16]={x:.55,y:wristY+asymmetry,visibility};landmarks[23]={x:.45,y:.65,visibility};landmarks[24]={x:.53,y:.65,visibility};landmarks[25]={x:.62,y:.72,visibility};landmarks[26]={x:.7,y:.72,visibility};landmarks[27]={x:.78,y:.72,visibility};landmarks[28]={x:.86,y:.72,visibility};return{timestampMs,landmarks}}
describe("Pilates The Hundred",()=>{
 it("counts arm pumps and cadence",()=>{const frames=Array.from({length:24},(_,index)=>frame(index*100,Math.floor(index/2)%2===0?.42:.48));const output=runPilatesHundredProtocol(frames);expect(output.status).toBe("accepted");expect(output.metrics.find((metric)=>metric.id==="hundred_arm_pump_count")?.value).toBe(5);expect(output.metrics.find((metric)=>metric.id==="hundred_arm_pump_cadence")?.value).toBeGreaterThan(100);expect(output.disclaimer).toContain("Não mede controle respiratório")});
 it("measures bilateral arm asymmetry",()=>{const frames=Array.from({length:24},(_,index)=>frame(index*100,Math.floor(index/2)%2===0?.42:.48,.02));const output=runPilatesHundredProtocol(frames);expect(output.metrics.find((metric)=>metric.id==="hundred_arm_asymmetry_mean")?.value).toBeGreaterThan(0)});
 it("rejects insufficient pumping amplitude",()=>{const output=runPilatesHundredProtocol(Array.from({length:24},(_,index)=>frame(index*100,.44)));expect(output.status).toBe("rejected");expect(output.reasons.some((reason)=>reason.code==="insufficient_depth")).toBe(true)});
});
