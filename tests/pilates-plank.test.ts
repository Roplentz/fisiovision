import{describe,expect,it}from"vitest";import{runPilatesPlankProtocol,type Landmark,type PoseFrame}from"../src/index.js";
function frame(timestampMs:number,hipY=.5,elbowShift=0):PoseFrame{const landmarks:Landmark[]=Array.from({length:33},()=>({x:.5,y:.5,visibility:.99}));landmarks[11]={x:.25,y:.42,visibility:.99};landmarks[13]={x:.22+elbowShift,y:.57,visibility:.99};landmarks[15]={x:.2,y:.7,visibility:.99};landmarks[23]={x:.52,y:hipY,visibility:.99};landmarks[27]={x:.86,y:.58,visibility:.99};return{timestampMs,landmarks}}
describe("Pilates Plank",()=>{it("accepts a sustained aligned plank",()=>{const out=runPilatesPlankProtocol(Array.from({length:12},(_,i)=>frame(i*200,.5+(i%2)*.002)));expect(out.status).toBe("accepted");expect(out.metrics.find(m=>m.id==="plank_valid_hold_duration")?.value).toBeGreaterThanOrEqual(2)});it("measures pelvic, trunk and elbow stability",()=>{const out=runPilatesPlankProtocol(Array.from({length:12},(_,i)=>frame(i*200,.5+(i===6?.012:0),i===5?.01:0)));expect(out.metrics.find(m=>m.id==="plank_pelvic_drift_max")?.value).toBeGreaterThan(0);expect(out.metrics.find(m=>m.id==="plank_elbow_angle_range")?.value).toBeGreaterThan(0)});it("rejects insufficient aligned hold",()=>{const out=runPilatesPlankProtocol(Array.from({length:12},(_,i)=>frame(i*200,i<4?.5:.72)));expect(out.status).toBe("rejected");expect(out.reasons.some(r=>r.code==="incomplete_cycle")).toBe(true)})});
