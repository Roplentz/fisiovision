import{describe,expect,it}from"vitest";import{runPilatesRollDownProtocol,type Landmark,type PoseFrame}from"../src/index.js";
function frame(timestampMs:number,lean:number,descent:number,visibility=.99):PoseFrame{const landmarks:Landmark[]=Array.from({length:33},()=>({x:.5,y:.5,visibility}));landmarks[7]={x:.5+lean,y:.18+descent,visibility};landmarks[11]={x:.5+lean,y:.3+descent,visibility};landmarks[23]={x:.5,y:.62,visibility};landmarks[25]={x:.5,y:.8,visibility};landmarks[27]={x:.5,y:.96,visibility};return{timestampMs,landmarks}}
describe("Pilates Roll Down",()=>{
 it("counts a complete roll down",()=>{const motion=[[0,0],[.02,.02],[.05,.05],[.1,.1],[.16,.15],[.1,.1],[.05,.05],[.02,.02],[0,0],[0,0],[0,0]];const output=runPilatesRollDownProtocol(motion.map(([lean,descent],index)=>frame(index*120,lean!,descent!)),{minimumRepetitionDurationMs:500});expect(output.status).toBe("accepted");expect(output.metrics.find((metric)=>metric.id==="repetition_count")?.value).toBe(1);expect(output.metrics.find((metric)=>metric.id==="roll_down_trunk_flexion_peak")?.value).toBeGreaterThan(20);expect(output.disclaimer).toContain("Não mede articulação segmentar")});
 it("rejects insufficient motion",()=>{const output=runPilatesRollDownProtocol(Array.from({length:12},(_,index)=>frame(index*100,.01,0)));expect(output.status).toBe("rejected");expect(output.reasons.some((reason)=>reason.code==="incomplete_cycle")).toBe(true)});
 it("rejects low visibility",()=>{const output=runPilatesRollDownProtocol(Array.from({length:12},(_,index)=>frame(index*100,.1,.1,.2)));expect(output.reasons.some((reason)=>reason.code==="low_visibility")).toBe(true)});
});
