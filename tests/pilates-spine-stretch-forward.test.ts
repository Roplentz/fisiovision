import{describe,expect,it}from"vitest";import{runPilatesSpineStretchForwardProtocol,type Landmark,type PoseFrame}from"../src/index.js";
function frame(timestampMs:number,flexion=0,kneeShift=0):PoseFrame{const landmarks:Landmark[]=Array.from({length:33},()=>({x:.5,y:.5,visibility:.99}));landmarks[23]={x:.5,y:.6,visibility:.99};landmarks[11]={x:.5+flexion,y:.35+flexion*.15,visibility:.99};landmarks[7]={x:.5+flexion*1.2,y:.25+flexion*.3,visibility:.99};landmarks[25]={x:.7,y:.62+kneeShift,visibility:.99};landmarks[27]={x:.88,y:.62,visibility:.99};return{timestampMs,landmarks}}
const motion=[0,0,.05,.1,.16,.1,.04,0,0,.06,.14,.06,0];
describe("Pilates Spine Stretch Forward",()=>{it("detects complete flexion-return cycles",()=>{const out=runPilatesSpineStretchForwardProtocol(motion.map((v,i)=>frame(i*200,v)));expect(out.status).toBe("accepted");expect(out.metrics.find(m=>m.id==="repetition_count")?.value).toBe(2)});it("measures flexion and knee stability",()=>{const out=runPilatesSpineStretchForwardProtocol(motion.map((v,i)=>frame(i*200,v,i===4?.02:0)));expect(out.metrics.find(m=>m.id==="spine_stretch_flexion_peak")?.value).toBeGreaterThan(20);expect(out.metrics.find(m=>m.id==="spine_stretch_knee_angle_range")?.value).toBeGreaterThan(0)});it("rejects an incomplete cycle",()=>{const out=runPilatesSpineStretchForwardProtocol(Array.from({length:12},(_,i)=>frame(i*150,i<3?0:.15)));expect(out.status).toBe("rejected");expect(out.reasons.some(r=>r.code==="incomplete_cycle")).toBe(true)})});
