import{describe,expect,it}from"vitest";import{recognizePilatesTeaser,type Landmark,type PoseFrame}from"../src/index.js";
function frame(t:number,valid=true):PoseFrame{const l:Landmark[]=Array.from({length:33},()=>({x:.5,y:.5,visibility:.99}));l[11]={x:.29,y:.3,visibility:.99};l[12]={x:.31,y:.31,visibility:.99};l[23]={x:.49,y:.6,visibility:.99};l[24]={x:.51,y:.61,visibility:.99};if(valid){l[25]={x:.64,y:.45,visibility:.99};l[26]={x:.66,y:.46,visibility:.99};l[27]={x:.79,y:.3,visibility:.99};l[28]={x:.81,y:.31,visibility:.99}}else{l[25]={x:.61,y:.48,visibility:.99};l[26]={x:.63,y:.49,visibility:.99};l[27]={x:.72,y:.7,visibility:.99};l[28]={x:.74,y:.71,visibility:.99}}return{timestampMs:t,landmarks:l}}
describe("Teaser position recognition gate",()=>{it("accepts a sustained balanced V with extended knees",()=>{const out=recognizePilatesTeaser(Array.from({length:40},(_,i)=>frame(i*100)));expect(out.accepted).toBe(true);expect(out.criteria.every(c=>c.passed)).toBe(true)});it("rejects the bent-knee exit",()=>{const out=recognizePilatesTeaser(Array.from({length:40},(_,i)=>frame(i*100,false)));expect(out.accepted).toBe(false);expect(out.failedCriteria).toContain("legs_raised");expect(out.failedCriteria).toContain("knees_extended")});it("rejects insufficient evidence",()=>expect(recognizePilatesTeaser([]).accepted).toBe(false))});