export type OpenMediaLicense="CC0-1.0"|"CC-BY-3.0"|"CC-BY-4.0"|"CC-BY-SA-4.0";
export interface OpenMediaRecord{id:string;exerciseId:string;title:string;sourcePage:string;downloadUrl:string;author:string;license:OpenMediaLicense;licenseUrl:string;commercialUseAllowed:true;attributionRequired:boolean;licenseReviewedAt:string;licenseReviewer:string;humanRightsReview:"required"|"completed";notes?:string}
export interface OpenMediaDecision{allowed:boolean;errors:string[]}
const HOSTS=new Set(["upload.wikimedia.org","archive.org"]);
export function validateOpenMediaRecord(record:OpenMediaRecord):OpenMediaDecision{const errors:string[]=[];let source:URL|undefined,download:URL|undefined;try{source=new URL(record.sourcePage);download=new URL(record.downloadUrl)}catch{errors.push("source and download URLs must be valid")}if(download&&!HOSTS.has(download.hostname))errors.push("download host is not approved");if(source?.protocol!=="https:"||download?.protocol!=="https:")errors.push("URLs must use HTTPS");if(!record.author.trim())errors.push("author is required");if(!record.licenseReviewer.trim()||!record.licenseReviewedAt.trim())errors.push("license review evidence is required");if(record.commercialUseAllowed!==true)errors.push("commercial use must be explicitly allowed");if(record.attributionRequired&&record.license==="CC0-1.0")errors.push("CC0 must not require attribution");if(record.license!=="CC0-1.0"&&!record.attributionRequired)errors.push("Creative Commons attribution license requires attribution");return{allowed:errors.length===0,errors}}
export function buildAttribution(record:OpenMediaRecord):string{return record.license==="CC0-1.0"?`${record.title} — ${record.sourcePage} — CC0 1.0`:`${record.title} — ${record.author} — ${record.license} (${record.licenseUrl}) — source: ${record.sourcePage}`}
