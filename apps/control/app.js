let releases=new Map(),active=null,history=[],events=[],keys=null;const errors=document.querySelector("#errors");
document.querySelector("#release").onchange=async e=>{try{const value=JSON.parse(await e.target.files[0].text());if(value.status!=="approved")throw new Error("Somente release aprovada pode ser registrada.");if(!value.id||!value.modelChecksum)throw new Error("Release incompleta.");releases.set(value.id,value);errors.textContent="";}catch(error){errors.textContent=error.message;}};
document.querySelector("#activate").onclick=async()=>{try{const release=[...releases.values()].at(-1);if(!release)throw new Error("Importe uma release aprovada.");if(active?.id===release.id)throw new Error("Release já está ativa.");if(active)history.push(active.id);const previous=active?.id;active=release;await audit("release_activated",release.id,previous);render();}catch(error){errors.textContent=error.message;}};
document.querySelector("#rollback").onclick=async()=>{try{const id=history.pop();if(!id)throw new Error("Não existe release anterior para rollback.");const target=releases.get(id);if(!target||target.status!=="approved")throw new Error("Release anterior não está aprovada.");const previous=active?.id;active=target;await audit("release_rolled_back",id,previous);render();}catch(error){errors.textContent=error.message;}};
async function audit(action,releaseId,previousReleaseId){const actor=value("actor"),reason=value("reason");if(!actor||!reason)throw new Error("Operador e motivo são obrigatórios.");if(!keys)keys=await crypto.subtle.generateKey({name:"ECDSA",namedCurve:"P-256"},false,["sign","verify"]);const payload={action,actorId:actor,releaseId,...(previousReleaseId?{previousReleaseId}:{}),reason,occurredAt:new Date().toISOString()},sequence=events.length+1,previousHash:events.at(-1)?.hash??null,hash=await digest(stable({sequence,previousHash,payload})),signature=toBase64(await crypto.subtle.sign({name:"ECDSA",hash:"SHA-256"},keys.privateKey,new TextEncoder().encode(hash)));events.push({schemaVersion:"fisiovision-audit-v0.1",sequence,previousHash,payload,hash,signature,keyId:"browser-p256-session"});}
function render(){document.querySelector("#active").hidden=false;document.querySelector("#releaseId").textContent=active.id;const config={schemaVersion:"fisiovision-consumer-config-v0.1",consumerId:value("consumer"),activeReleaseId:active.id,engineVersion:active.engineVersion,protocolId:active.protocolId,protocolVersion:active.protocolVersion,modelName:active.modelName,modelVersion:active.modelVersion,modelChecksum:active.modelChecksum,activatedAt:new Date().toISOString()};document.querySelector("#config").innerHTML=Object.entries(config).map(([k,v])=>`<span>${safe(k)}: <b>${safe(v)}</b></span>`).join("");document.querySelector("#audit").innerHTML=events.map(e=>`<div class="event"><b>#${e.sequence} · ${e.payload.action}</b><br><small>${safe(e.payload.actorId)} · ${safe(e.payload.reason)}</small></div>`).join("");}
document.querySelector("#export").onclick=async()=>{if(!events.length||!keys)return;const publicKey=await crypto.subtle.exportKey("jwk",keys.publicKey),bundle={schemaVersion:"fisiovision-audit-export-v0.1",publicKey,events};download("fisiovision-audit.json",JSON.stringify(bundle,null,2));};
function stable(v){if(Array.isArray(v))return"["+v.map(stable).join(",")+"]";if(v&&typeof v==="object")return"{"+Object.entries(v).sort(([a],[b])=>a.localeCompare(b)).map(([k,x])=>JSON.stringify(k)+":"+stable(x)).join(",")+"}";return JSON.stringify(v)??"null"}async function digest(v){const b=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v));return[...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,"0")).join("")}function toBase64(buffer){return btoa(String.fromCharCode(...new Uint8Array(buffer)))}function value(id){return document.querySelector("#"+id).value.trim()}function safe(v){const n=document.createElement("span");n.textContent=String(v);return n.innerHTML}function download(name,text){const u=URL.createObjectURL(new Blob([text],{type:"application/json"})),a=document.createElement("a");a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
