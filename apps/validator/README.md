# FisioVision Validator

Aplicação estática para consolidar pacotes gerados pelo Reviewer.

Execute `npx serve apps/validator`, selecione vários arquivos `fisiovision-validation-package-v0.1` e exporte o relatório geral em Markdown, CSV ou JSON.

Os arquivos são lidos apenas pelo navegador e não são enviados.
